@model IEnumerable<Itransition.Models.Attributes.AttributeOption>

@{
    ViewData["Title"] = "Attribute Options";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2><i class="bi bi-list-check me-2"></i>Attribute Options</h2>
</div>

<div class="mb-3">
    <a asp-action="Create" class="btn btn-primary btn-sm">
        <i class="bi bi-plus-lg me-1"></i>Create New Option
    </a>
</div>

<div class="table-responsive shadow-sm rounded">
    <table class="table table-hover align-middle mb-0">
        <thead class="table-dark">
            <tr>
                <th>Option Value</th>
                <th>Attribute Category</th>
            </tr>
        </thead>
        <tbody>
    @foreach (var item in Model) {
            <tr>
                <td>
                    <a asp-action="Details" asp-route-id="@item.Id" class="fw-bold text-decoration-none">@item.Value</a>
                </td>
                <td>
                    <span class="badge bg-secondary">@(item.AttributeDefinition?.Category?.Name ?? "N/A")</span>
                </td>
            </tr>
    }
        </tbody>
    </table>
</div>
